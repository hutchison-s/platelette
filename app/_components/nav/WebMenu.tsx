import NavLinks from "./NavLinks";

function WebMenu() {
    return (
        <nav className="text-white font-body gap-4 p-4 hidden sm:flex">
            <NavLinks />
        </nav>
    )
}

export default WebMenu;